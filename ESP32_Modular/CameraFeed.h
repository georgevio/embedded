#ifndef CAMERAFEED_H
#define CAMERAFEED_H

#include "Config.h"

void setupCamera() {
  // Placeholder for camera setup code
  const char configMsg[] = "Cam not yet";
  SERIAL_PRINTLN(F(configMsg));
}

void handleCameraFeed() {
  // Placeholder for camera feed handling code
  const char configMsg[] =  "Cam Handle not yet";
  SERIAL_PRINTLN(F(configMsg));
}

#endif // CAMERAFEED_H
